name: Swift Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Run all Swift package tests
        run: |
          chmod +x scripts/test-all.sh
          ./scripts/test-all.sh
