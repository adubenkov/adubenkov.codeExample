# AGENTS.md — карта проекта для агентов (шаблон посева)

> Посеян `process-init` (аудит №4: самый цитируемый ролями документ отсутствовал
> в каноне — 12 упоминаний в шести ролях вели в пустоту). Замени заглушки
> `<...>` на своё; структуру и «Гигиену shell-команд» сохраняй — на них
> ссылаются роли.

Проект: публичная витрина Swift/iOS-примеров для найма — продакшен-паттерны,
тесты, инженерные стандарты; расширение резюме владельца.
Стек: Swift / SPM, GitHub Actions; роль разработки: `ios-dev`.

## Карта доков (прочти нужное ДО работы, не вместо неё)

| Документ | Что там |
|---|---|
| docs/TEAM.md | регламент конвейера (копия канона dev-process) |
| docs/STATUS.md | что происходит сейчас — ведёт manager |
| docs/specs/BACKLOG.md | идеи, chore-очередь, побочные находки |
| docs/stack/*.md | конвенции сборки/тестов/сервисов твоего стека |
| docs/CodeStyle.md, docs/Evolution.md | стандарты и эволюция витрины |
| docs/InterviewWalkthrough.md | как витрину читает нанимающий — держи её такой |

## Зоны ролей

Каталоги, которыми владеет каждая пишущая роль (дифф PR обязан соответствовать
зоне автора; на эту таблицу ссылаются роли канона):

| Роль | Каталоги зоны |
|---|---|
| `ios-dev` | `examples/`, `scripts/`; `legacy/` — только чтение |
| devops | `.github/workflows/` |

## Риск-триггеры (повышают категорию PR до «рисковый»)

Семантические триггеры, которых не видит таблица путей (`docs/process/pr-category.tsv`);
manager применяет их суждением — только вверх:

- публичность: внутренние имена, чужой код без лицензии, любые секреты = BLOCK
- изменения в `legacy/` (исторический слой, трогается отдельным решением)
- переименование/перенос примеров (ломает внешние ссылки из резюме)
- CI-воркфлоу (бейдж в README — витрина зелёности)

## Гигиена shell-команд (канон; нарушение = permission-промпт у владельца)

1. Поиск по коду — инструменты Grep/Glob/Read; нет в тулсете — плоский
   `git -C <репо> grep -n <шаблон>`. Голые shell-`grep`/`find`/`sed`/`awk`
   для поиска запрещены.
2. Bash — плоские одиночные команды. Запрещены: `$` в любом виде, `$( )`,
   backticks, циклы, `find -exec`, `cd` в составных командах, конгломераты
   `cmd; echo ===; cmd`. Одна команда — один вызов Bash.
3. Правки файлов — только Edit/Write, не пишущий shell.
4. Многострочные тексты (тела PR, комментариев, issue) — Write в файл в
   скратчпаде + `--body-file` (markdown с backticks инлайном триггерит промпт).
5. `.claude/settings.json` и файлы ролей не трогаешь (режется хуком);
   недостающее правило allowlist — текстом в финальный отчёт.
6. Ветка порции — `task/<слаг>` от `origin/main`; push после КАЖДОГО коммита;
   force-push, shallow-fetch и массовый `git add` запрещены (режется хуком).
7. Деструктив (удаление данных, пересоздание томов, живые внешние сервисы) —
   только по явному запросу владельца.

## Прогоны — только синхронно

Фоновые задачи не создавать: уведомление о завершении фонового ребёнка будит
главную сессию, а не тебя — конвейер встаёт. Сборки/тесты форграундом с явным
timeout; финальное сообщение — только РЕЗУЛЬТАТ, никогда «жду».

## Скиллы (вызываются ДО работы, front-load в постановке)

| Зона | Скиллы |
|---|---|
| — | скиллов в проекте пока нет; таблица заполняется при их появлении |

## Экономия токенов

Отчёты — одной строкой фактов (SHA, номера PR, счёт тестов); реле без
пересказов; читай секции доков по оглавлению, не файлы целиком; стабильную
часть промпта держи в начале, изменчивое — в конце (кэш).
